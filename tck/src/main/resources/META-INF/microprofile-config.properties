# The TCK SUT has no TaskAuthorizationProvider, and a2a-java 1.3.0.Final denies every task operation
# when none is configured (fail-closed). The upstream a2a-java TCK application does the same in its
# application.properties.
a2a.authorization.required=false
